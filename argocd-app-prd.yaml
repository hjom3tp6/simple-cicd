apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: myapp-prd
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/hjom3tp6/simple-cicd
    targetRevision: main
    path: infra/helm/myapp
    helm:
      releaseName: myapp-prd
      valueFiles:
        - values.yaml
        - values-prd.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: prd

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
